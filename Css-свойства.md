Свойство position значения:
`static` — обычный поток,  
`relative` — смещение относительно себя,  
`absolute` — относительно ближайшего positioned-родителя,  
`fixed` — относительно viewport,  
`sticky` — прилипает при скролле.
