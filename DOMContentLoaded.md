`DOMContentLoaded` — это момент, когда DOM готов к использованию, но ресурсы ещё могут загружаться.

**HTML полностью загружен и разобран, DOM-дерево построено и готово к работе.**

При этом **картинки, видео и стили могут ещё загружаться**.

Зачем нужен: безопасно работать с DOM, не ждать полной загрузки страницы.

```txt
Порядок загрузки станицы (упрощённо):

1. HTML загружается
2. HTML парсится → DOM
3. defer-скрипты выполняются
4. 🔔 DOMContentLoaded
5. Картинки / стили / шрифты догружаются
6. load
```

Влияние скриптов: `DOMContentLoaded` ждёт default, defer-скрипты, async - не ждёт. 