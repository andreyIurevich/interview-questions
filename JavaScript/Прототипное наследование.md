В JavaScript объекты имеют специальное скрытое свойство `[[Prototype]]` (как указано в спецификации), которое равно либо null, либо ссылается на другой объект. Этот объект называется `прототип`

Когда необходимо прочитать свойство из объекта, а оно отсутствует, то оно берётся из прототипа. Такой механизм называется `прототипным наследованием`

Свойство `[[Prototype]]` внутреннее и скрытое, но есть несколько способов его задать:

1. Свойство `__proto__` - геттер/сеттер для `[[Prototype]]` (используется только для чтения свойств)
2. Использовать функции `Object.getPrototypeOf/Object.setPrototypeOf`

У функций (за исключением стрелочных) есть обычное свойство `F.prototype` Это ещё не «прототип объекта», а обычное свойство `F` с таким именем. По умолчанию `"prototype"` – объект с единственным свойством `constructor`, которое ссылается на функцию-конструктор. Свойство `F.prototype` (не путать с `[[Prototype]]`) устанавливает`[[Prototype]]` для новых объектов при вызове `new F()`.

```unset
    [ 1. ФУНКЦИЯ-КОНСТРУКТОР ] 
    ┌────────────────────────┐
    │ function Animal(name)  │
    │                        │             [ 2. ОБЪЕКТ-СКЛАД (Прототип) ]
    │   .prototype ──────────┼────────────►┌────────────────────────────┐
    └────────────────────────┘             │  {                         │
        ▲                                  │    constructor: Animal, ───┼──┐
        │                                  │    walk: function() {...}  │  │
        │                                  │  }                         │  │
        │                                  └────────────────────────────┘  │
        │                                       ▲                          │
        │ Навешивает ссылку __proto__           │                          │
        │ в момент вызова `new Animal()`        │                          │
        │                                       │                          │
        │                                       │                          │
    [ 3. СОЗДАННЫЙ ОБЪЕКТ ]                     │                          │
    ┌────────────────────────┐                  │                          │
    │ const dog = {}         │                  │                          │
    │                        │                  │                          │
    │   name: "Рекс",        │                  │                          │
    │   __proto__ ───────────┼──────────────────┘                          │
    └────────────────────────┘                                             │
        ▲                                                                  │
        └──────────────────────────────────────────────────────────────────┘
                 dog.constructor по цепочке возвращает Animal
```

## Как читать эту схему (3 главных правила):

1. `.prototype` принадлежит только Функции:  
    Это свойство-ориентир. Самой функции `Animal` эти методы (например, `walk`) не нужны. Она держит этот объект-склад `[2]` исключительно для своих будущих "детей".
2. `__proto__` принадлежит созданным Объектам:  
    Когда ты пишешь `new Animal("Рекс")`, движок JS создает пустой объект `dog`, записывает туда имя, а затем копирует ссылку из `Animal.prototype` в скрытое свойство `dog.__proto__`. Теперь объект `dog` напрямую связан с объектом-складом `[2]`.
3. Замыкание круга через `constructor`:  
    Поскольку объект-склад `[2]` изначально имеет свойство `constructor`, указывающее назад на `Animal`, объект `dog` получает к нему доступ. Когда ты пишешь `dog.constructor`, JS не находит его в самом `dog`, прыгает по стрелочке `__proto__` в объект-склад, находит его там и возвращает функцию `Animal`.


