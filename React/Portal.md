**React Portals** — это механизм, который позволяет **рендерить JSX в другой узел DOM**, находящийся **вне иерархии родительского компонента**, при этом **логически компонент остаётся частью React-дерева**.

```jsx
ReactDOM.createPortal(
  <Modal />,
  document.getElementById('modal-root')
);
```

📌 Компонент:
- находится **в другом месте DOM**
- но участвует в **React context, state и events**

Проблема без порталов: overflow: hidden, z-index

**События всплывают по React-дереву, а не по DOM**

```jsx
<div onClick={() => console.log('parent')}>
  {createPortal(
    <button onClick={() => console.log('button')} />,
    modalRoot
  )}
</div>

при клике: 
button
parent

Даже если кнопка физически в другом месте DOM
```

**Как порталы работают с контекстом**

```jsx
<ThemeContext.Provider value="dark">
  <Modal />
</ThemeContext.Provider>
```

`Modal` внутри портала **получит контекст**

## Краткое резюме

🔹 Portal = другой DOM, тот же React  
🔹 Решает проблемы overlay / z-index  
🔹 События всплывают по React-дереву  
🔹 Контекст сохраняется  
🔹 Незаменим для модалок и тултипов
